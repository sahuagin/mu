# mu — session handoff 2026-05-24T00:22Z (~2026-05-23 20:22 EDT)

Long session. Restart imminent. This is the state for the next session to pick up cleanly without re-deriving anything.

## What this session was about

- Carried `/btw` from `/handoff` design discussion into a full architectural pass on mu's exogenic-interop layer.
- Designed mu's HANDOFF wire format, exo-adapter registry, foreign-runtime-wrapper, /handoff skill, and skill system.
- Filed eight new beads + one bead update + one SKILL.md fix.
- **Wasted ~$8–10 of Max5x subscription trying to launch the mu-nbi0 autonomous goal session.** Failure mode is structural, not bug-shaped — see "DO NOT RE-DO" below.

## Beads filed (this session)

| ID | Title | Status |
|----|-------|--------|
| mu-t71p | mode-indicator UX (glyphs 無/·/!/→/↳, color, /help catalog) | open |
| mu-u6hc | `mu context` OS-memory-map view (events-JSONL derived) | open |
| mu-1rmo | mu skill system (slash dispatch + SKILL.md loader + body injection) | open |
| mu-xtid | claude-code wrap-and-compact prototype findings | **closed** |
| mu-nt7j | HANDOFF wire-format spec (JSON-canonical, prose-rendered) | open |
| mu-p49v | exo-runtime protocol adapter (registry + bootstrap + verifier) | open |
| mu-psf6 | foreign-runtime wrapper (shadow event log + /clear-and-paste) | open |
| mu-01ng | /handoff skill in mu (wire-format exporter) | open |
| mu-nbi0 | claude-code session-log mining → exo-protocol catalog | open (sub-beads mu-3xaa, mu-p8p9, mu-ht2t) |
| mu-zhcy | goal-protocol pre-flight permission+reachability smoketest | open |

Dependency graph: `mu-xtid → mu-p49v`, `mu-nbi0 → mu-p49v`, `mu-nt7j → {mu-psf6, mu-01ng}`, `mu-p49v → mu-psf6`, `mu-1rmo → mu-01ng`, Phase A→B→C sub-bead chain under mu-nbi0.

## Memories saved (this session — substantive ones)

- `3476ccc2` (feedback) — **mode_indicator_calibration**: loudness ∝ cost-of-forgetting, capped at unpleasant-to-work-in. ATD red-prod-terminal principle.
- `5a4e0ebc` (reference) — **claude_code_print_mode_observability**: `claude -p` text mode buffers; use `--output-format=stream-json --verbose`.
- `6fa52297` (feedback) — **goal_protocol_permission_preflight_gap**: `--permission-mode bypassPermissions` required for autonomous /goal; permission profile pre-flight check needed.
- `e4452430` (reference) — **goal_protocol_skill_remediation_landed_2026_05_23**: the two bugs flagged in 2026-05-20 postmortem are now fixed in SKILL.md.
- `e10884e5` (reference) — **nohup_claude_from_claude_inherits_uds_stdin**: spawning claude-code via Bash from inside another claude-code inherits parent's UDS as stdin; needs `< /dev/null`.
- `7cb90de2` (reference) — **claude_code_needs_complete_linux_mounts**: host `/compat/linux` is too sparse; futex spin at startup; must run inside jail with full mounts.

## SKILL.md remediation landed

Two targeted edits to `~/.claude-personal/skills/goal-protocol/SKILL.md` fixed the bugs flagged by the 2026-05-20 forge-rs postmortem (`--goal` flag illusion + missing `--permission-mode bypassPermissions` guidance). Memory `e4452430` documents.

## DO NOT RE-DO

These cost real money or time today. Don't relitigate without new evidence:

1. **autonomous claude-code is structurally blocked on this machine.** Memory `cc506f71` from 2026-05-18 (validated; re-confirmed today): claude-code on the host fails with futex spin (impoverished `/compat/linux`); claude-code in a pot fails with unkillable Bun fs.watch deadlock (`fs.watch` thread ignores SIGKILL, pot recoverable only by `pot stop`). Five candidate workarounds enumerated, none chosen. **Status: parked.**
2. **mu-nbi0 (log mining) goal session attempts.** Tried 9 different invocations today. None completed. Cost ~$8–10. The artifacts (`launch.sh`, `goal-text.md`, experiment doc) are preserved at `~/.claude-personal/experiments/goal-2026-05-23-mu-nbi0.*` and `~/.claude-personal/experiments/goal-2026-05-23-mu-log-mining.md`. **Three honest paths** for actually getting the catalog:
   - **Pivot to mu (Rust runtime) via `agent-spawn-v2`** — validated 2026-05-18 in `3b894675`. Needs mu autonomous-loop robustness check first.
   - **Do mu-nbi0 interactively in a claude-code session** — well-defined work, ~hour or so.
   - **Park until Bun-on-FreeBSD-Linuxulator fs.watch is fixed.**
3. **The 6/15 cliff is NOT a paradigm shift.** Memory `1c0cd76d`: `claude -p` moves to a $100/mo Max5x credit pool, not to direct API billing. Interactive claude-code stays on subscription pool unchanged. The architectural reframe (MCP push primitive, mu↔claude-code via MCP) is still good but not *forced* by economics.
4. **HANDOFF file is exogenic interop only.** mu native uses event-log resume, internal compaction, and `session.delegate` for everything else. Don't design native-mu features around the HANDOFF artifact.

## Pending decisions you flagged

1. Rename `MORNING_<date>.md` → `HANDOFF_<datetime>.md` across goal-protocol skill, mu specs, `~/.local/bin/jj-orphan-audit`. **Approved, not yet executed.** Will bundle with phase 2.5 of /handoff when 2.5 design lands. (This file uses the new convention.)
2. JSON-canonical companion for HANDOFF artifact (phase 2.5). Approved direction.
3. Smoketest bead (mu-zhcy) should additionally validate "spawning from inside claude-code adds `< /dev/null`" per memory `e10884e5`.
4. "I'd like to discuss /handoff to see if there is a phase 2.5 we want to consider" — you had more design coming on this. Not yet landed.

## Working tree state

- Repo: `/home/tcovert/src/public_github/mu`
- Branch: `main`
- Dirty: `.beads/issues.jsonl` only (bead inserts from this session, ~10 new entries)
- No code edits this session in the mu repo. The SKILL.md edit was outside the mu repo.

## To resume

1. `cd /home/tcovert/src/public_github/mu`
2. `agent memory context --cwd $PWD` (probably auto-fires via SessionStart hook)
3. Read this file
4. Pick from "Pending decisions" or move forward on the bead landscape (mu-1rmo + /handoff backports are probably the highest-value next moves on the front end; mu-u6hc Phase A/B is the cleanest standalone bead)
5. **Do NOT attempt to launch mu-nbi0 as an autonomous /goal session via claude-code.** Either do it interactively or pivot to mu via agent-spawn-v2.

## One operational note

I'm at 51% context and rediscovering things from earlier in the same session. The user (tcovert) called this out and asked to restart cleanly. Treat memories as canonical when they exist; don't relitigate.
