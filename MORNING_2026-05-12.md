# Morning briefing — 2026-05-12

Overnight session continued from the evening's `mu-pex` + TUI ergonomics work. 23 commits total across the long session; 7 of those landed overnight (after you said "goodnight"). All workspace tests green throughout — no broken commits to revert.

## What landed overnight

In commit order on `@-` chain (most recent first):

| Commit | Bead | One-line |
|---|---|---|
| `vzrtrywv 755f3fd8` | `mu-28u` (open) | mu-036 **Phase A.2**: wire types + EventPayload variants + dispatch stubs |
| `vwuuoxym 03bf367b` | `mu-28u` (open) | mu-036 **Phase A.1**: `AutonomyCapability` + attenuate intersection |
| `qkxtkovn 4fbc54dc` | `mu-935` ✓ | `FileBackend` for cross-daemon session listing (reads mu-upb JSONLs) |
| `zlsyywlu a5bcbf47` | `mu-62s` ✓ | TUI `:system_prompt` palette + Ctrl-X Ctrl-P `$EDITOR` chord |
| `woxzrvow e87b636b` | `mu-yyi` ✓ | GrepTool inject `rg_path`, drop env-mutation test race |

(Evening commits — already discussed before you went to bed — also part of the chain: `xollnzmv` mu-n48, `qzqvyork` F3 picker, `tvloxyyo` mu-ium, `xzswppyz` mu-jb3, `kwlmtlxm` mu-82l, `powkzxrl` mu-wd2+mu-h04, `upsomnkn` mu-pex smoke test, `sunvrnkp` mu-pex bug fix, `lrslqwuv` mu-pex Phase 1.5, `rwyyuout` mu-pex Phase 1, `soslotnq` mu-yyi triage, `kqxtnwot` mu-ooy.)

## Test state

```
Workspace tests: 319 passed, 0 ignored, 0 failed
```

The previously-`#[ignore]`d `rg_unavailable_returns_clean_error` now runs cleanly in parallel (mu-yyi). +7 tests for `AutonomyCapability::intersect` math. +5 tests for `FileBackend` peer-scan. Live mu-pex smoke test against anthropic-api still green.

## Things you can try

1. **`:system_prompt`** in the TUI — sets a default system prompt that the next `n` carries into `create_session`. Cache% in F5 should rise on multi-ask sessions when set. Ctrl-X Ctrl-P (in input mode) opens `$EDITOR` for multi-line editing. Header shows `sys:✓` when set.
2. **Cross-daemon session discovery**: if you run two `mu serve` daemons against the same `~/.local/share/mu/events/` and call `session.list` with `include_remote: true` on one of them, you'll see the other's sessions as `is_remote: true` rows (works once their events_dir is shared — the launch command already gives you this).
3. **Autonomous loop wire surface**: clients can now send `session.start_autonomous` and `session.schedule_wakeup` requests; they'll get structured `"Phase B not yet wired"` errors. The capability gate (`AutonomyCapability::Disallowed`) is real — try setting a session's capability and the rejection is the right shape.

## mu-28u (autonomous loop) — where it stands

**Phase A (foundation, no behavior)** is complete. Code can express autonomy capabilities, attenuate them through delegations, and validate wire-protocol requests. Dispatch handlers reject with structured errors that explicitly cite the missing phase bead.

**Phases B–D remain open** as their own beads:

- **mu-3ao** (P2, Phase B): `RunMode` enum + `handle_start_autonomous` agent-loop integration + SelfReport goal-check. Probably 300-500 LOC including FauxProvider-driven tests. **This is the meat of the feature.**
- **mu-7zn** (P3, Phase C): `schedule_wakeup` + `RunMode::Sleeping`. Adds a tokio sleep future + wake channel to the agent loop. ~200 LOC.
- **mu-pv9** (P3, Phase D): DelegateGrader + ExternalSignal goal-check methods. ~300 LOC.

I'd push **mu-3ao first** when you next sit down with it. The wire surface from Phase A.2 means clients work the moment Phase B lands; D can wait indefinitely.

## Things noticed but not addressed

These accumulated during the overnight run; worth knowing about:

1. **AgentEvent variants matching the new `EventPayload` autonomy variants are NOT yet added.** The `forwarder::to_log_event` mapping is part of Phase B; for now `AgentEvent::Autonomous*` doesn't exist. Phase B's first job is adding both sides simultaneously.
2. **The bash-tool env-mutation test pattern from mu-yyi isn't fixed** — only GrepTool was refactored. BashTool's `b5_strict_env_scrub` and `b10_yolo_env_passes_through` still mutate process env; they don't currently race against anything else (no other test reads `MU_TEST_BASH_*` keys) so it's latent risk only. If it ever bites, the same inject-config pattern applies.
3. **`SessionEventLog::from_jsonl`** (added for FileBackend) is also the load-bearing primitive for mu-mh4 (session persistence across daemon restart). When mu-mh4 lands it can use this directly.
4. **Provider trait gained `system_prompt: Option<&str>` overnight (mu-n48)** — any new provider impl needs to handle it. Anthropic uses content-block + cache_control; OpenAI-style prepends a system message; faux ignores.
5. **F5 dashboard's `tool p95` column** (mu-jb3) is populated. The dashboard updates in real-time on session.done — try a long-tool-using session and watch the per-(provider, model) row evolve.
6. **Eight new follow-up beads filed overnight**:
   - `mu-3ao`, `mu-7zn`, `mu-pv9` — autonomous loop phases B/C/D
   - `mu-i6j` ✓, `mu-vck`, `mu-8m0`, `mu-n48` ✓, `mu-62s` ✓ — various from the evening's design discussion (✓ = closed; rest open)

## Discipline I kept

- Every commit was a `cargo test --workspace` green checkpoint. No broken-state commits to revert.
- Every closed bead got a `task_log add` entry so `task_log query --cwd ~/src/public_github/mu` will show the run.
- Every multi-phase piece (mu-036) got its phases filed as separate beads so the morning view is "which phases done, which not."
- GPT/codex backend stayed mostly unused — I tried delegating mu-036 spec analysis but `agent-router --backend codex-oauth` couldn't find `mu` on PATH and only produced a placeholder `cat` command. Switched back to direct reading rather than fight the setup. **Worth a future bead**: add the mu release binary to a stable path (or symlink to `~/.local/bin/mu`) so agent-router's codex path can find it.

## Suggested next session

1. **Try the overnight features** — `:system_prompt`, the F5 dashboard with a long ask, F3-picker with multiple providers.
2. **Pick `mu-3ao` if you want to push mu-036 forward** — Phase B is the rewarding piece; Phase A laid the rails.
3. **Or sweep small follow-ups**: file the "mu binary on PATH for agent-router codex backend" bead, file a "BashTool env-mutation followup" bead if you want defensive coverage of the latent race.

Solid push. Sleep well already over with — coffee well-earned.
