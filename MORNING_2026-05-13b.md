# mu: goal session 2026-05-13b — experiment 2 (mu-3ao Phase B finish + stretches)

## Outcome
**Status: COMPLETE (primary + both stretches landed).** Phase B (mu-3ao) closed; rope spec amendment (mu-qk8) closed; 3 beads filed from the 2.1.139 mapping. cargo test --workspace green at every commit. Working copy clean of code changes (MORNING_2026-05-13.md + specs/process/goal-protocol.md from experiment 1 remain uncommitted; not Phase-B concerns).

## What landed

| Commit | Bead | One-line |
|---|---|---|
| nnzmzx e443da4e | mu-3ao | feat(autonomy): mu-036 Phase B — autonomous loop + handle_start_autonomous (closes mu-3ao) |
| qvnwpy d6d25b44 | mu-qk8 | docs(architecture): amend event-sourced-context.md with 6 rope extensions (closes mu-qk8) |
| (no commit; br only) | mu-ikz, mu-hlf, mu-rdr | 3 beads filed from claude-code 2.1.139 → mu mapping |

## Test state
**Green.** `cargo test --workspace` reported 14 "test result: ok" lines (totaling ~225 tests including 107 in mu-coding + 117 in mu-core + smaller crates) at both commits. The three new autonomous tests (`a1_iteration_cap_terminates_at_capability_bound`, `a2_self_report_goal_met_early_termination`, `a3_disallowed_defensive_callout_no_autonomy_events`) pass with INV-7 assertions embedded in a1/a2.

## Goal status
- **mu-bqj** (tracking bead): **CLOSED** — see commit/bead summary below.
- **mu-3ao**: **CLOSED** at commit nnzmzx e443da4e.
- **mu-qk8**: **CLOSED** at commit qvnwpy d6d25b44.
- Sub-beads closed: 2 (mu-3ao, mu-qk8)
- Sub-beads still open: none required by mu-bqj.
- Sub-beads filed during session: 3 — mu-ikz, mu-hlf, mu-rdr (claude-code 2.1.139 mapping, stretch B).

## Punch-list completion (vs MORNING_2026-05-13.md)

| # | Item | Status |
|---|---|---|
| 1 | forwarder.rs to_log_event: 3 arms for new AgentEvent autonomy variants | ✅ done |
| 2 | loop_tests.rs kind(): 3 arms returning static `&str` | ✅ done |
| 3 | dispatch.rs handle_start_autonomous: replace Phase-A.2 stub | ✅ done (now async, sends AgentInput::StartAutonomous, returns accepted=true) |
| 4a | FauxProvider test: iteration cap enforcement (3 starts, 3 completes, 1 Terminated{IterationCap}) | ✅ done — `a1_iteration_cap_terminates_at_capability_bound` |
| 4b | FauxProvider test: SelfReport early termination at iteration 2 | ✅ done — `a2_self_report_goal_met_early_termination` |
| 4c | INV-7: AutonomousTerminated is the LAST autonomy event in every test path | ✅ done — asserted inside a1 and a2 |
| 4d | AutonomyCapability::Disallowed defensive callout, mode stays Idle | ✅ done — `a3_disallowed_defensive_callout_no_autonomy_events` |
| 5 | `cargo test --workspace` green | ✅ |
| 6 | Single bead-closing commit citing spec mu-036:132-150 in body | ✅ — commit body cites `specs/mu-036-session-autonomous-loop.md` §wire-notifications lines 132-150 |

## Stretches

### Stretch A — rope spec amendment (mu-qk8 closed)
Applied 204-line draft from `~/.claude-personal/drafts/mu-spec-amendment-event-sourced-context.md` to `specs/architecture/event-sourced-context.md`. Six new sections (pointer-set + skills/tools, cache-boundary alignment, pluggable provider/cache strategies, agent-view vs operator-view projections, subagent context handoff, source-change file watches). Cross-references updated to include memory `3cc7a18c` and the five follow-on beads (mu-ktq, mu-nat, mu-ovl, mu-x9j, mu-56p). Changelog entry added. **One doc-only commit; no Rust changes; cargo test still green.**

### Stretch B — 3 beads from claude-code 2.1.139 mapping
- `mu-ikz` — Authority-coloring as refinement on top of state-coloring (refines mu-pex). Authority tier as a second visual axis (border/label) atop the state-coloring (fill). Coarse palette per memory `5ff7511e`.
- `mu-hlf` — Profile-driven file-edit isolation (refines mu-627). `FileIsolation { None | Worktree { root } | ReadOnly }` as a property of session profile, not hardcoded to background-only.
- `mu-rdr` — `--bare`-equivalent boot mode for mu. Minimal-context boot for delegates / sub-sessions, declarative as a different initial retained set (per the rope amendment in mu-qk8).

These three beads constitute the 2.1.139 → mu mapping called out in memory `fb5cd5be`. **No commits in mu** (br updates only — already part of qvnwpy issues.jsonl change).

## Stop criteria that fired
**None.** Both the seven defaults and the three mu-specific additions stayed quiet. Detailed audit:

| # | Criterion | Fired? | Notes |
|---|---|---|---|
| 1 | Test failure not obvious in 3 iter | N | No test failures during the session. |
| 2 | Cross-cutting architectural | N | All work in the 4-5 file surface; doc amendment is purely architectural (no trait churn). |
| 3 | Capability invariant weakening | N | INV-1, INV-2, INV-3, INV-6, INV-7 explicitly verified — see Capability invariant audit below. |
| 4 | Unforeseen load-bearing block | N | Punch list executed clean. |
| 5 | Codex 3-strike | N | No codex delegations attempted (the work was small enough to do directly). |
| 6 | Concurrent-session divergence | N | Inherited dirty state was the expected experiment-2 starting point. |
| 7 | Concern-mixing commit | N | Phase B + spec amendment split via `jj split` — clean separation, two commits. |
| 8 (mu) | Capability invariant violation | N | Same as #3. |
| 9 (mu) | Spec drift on Provider/wire/EventPayload change | N | Wire structs implement existing spec prose (mu-036 §wire-notifications:132-150). Commit body cites spec. |
| 10 (mu) | Generated-file edit | N | No generators in scope. |

## Things noticed but not addressed
- **The continuation-prompt between iterations is still a UserMessage stub** ("iteration N: continue toward the goal"), whereas mu-036 §implementation-sketch suggests a system message. Flagged in experiment 1 briefing; deferred again. Worth a small follow-up bead if real-model autonomy surfaces ambiguity.
- **`extract_goal_status` uses a naive `{...}` substring scan** — fine for FauxProvider; revisit if a real model emits nested JSON that confuses the parser.
- **The MutexGuard-across-await fix in `Action::External(StartAutonomous)`** was a small refactor (snapshot the autonomy capability via clone + immediate drop) that the prior session left uncovered. The bug would only have triggered the compiler check; behavior was correct. Worth a memory if similar guards-across-await patterns recur elsewhere.
- **MORNING_2026-05-13.md (experiment 1 briefing) and specs/process/goal-protocol.md remain uncommitted in the working copy.** They're inherited artifacts from experiment 1; not Phase-B concerns. A follow-up commit can land them as a docs/process commit; deferred to avoid concern-mixing in this session.
- **Memory CLI semantic search is broken** (`Error: no such column: code` from `agent memory search`). Recent commands still work. Worth filing as a personal-bead if not already.

## Suggested next session
1. **Commit MORNING_2026-05-13.md + specs/process/goal-protocol.md** as a docs commit (separate bead — or a no-bead "session-infrastructure" commit if tolerated).
2. **mu-036 Phase C (mu-7zn)**: implement `RunMode::Sleeping` + `session.schedule_wakeup` per spec §wake-up-scheduling. Phase B left the `Sleeping` variant declared but unused. Spec section is ready.
3. **mu-036 Phase D (mu-pv9)**: `DelegateGrader` + `ExternalSignal` goal-check methods. Spec section ready.
4. **Continuation-prompt refinement** in Phase B (UserMessage → system message between iterations). Small bead.
5. **`extract_goal_status` robustness**: handle nested JSON correctly. Small bead.
6. **Implementation of mu-qk8 follow-ons**: mu-ktq (renderer/cache traits) is the closest to "small enough to start" of the five follow-on beads.
7. **Authority-coloring (mu-ikz) + file-isolation (mu-hlf) + bare-boot (mu-rdr)** can be picked up in any order; they are independent of each other and of the rope amendment work.

## Cost / turns / wall-clock
- Cost: **~$8.16** of $15 cap at termination start (well within budget; ~$9 remaining after this briefing lands).
- Wall-clock: ~one session (single conversation, no compaction).
- Codex delegations: 0
- Subagent dispatches: 0 (work was small enough to do directly per goal-protocol guidance)
- LOC delta: ~280 LOC of Phase B code (committed) + ~190 LOC of spec amendment (committed) + ~80 LOC of test code (committed).

## Capability invariant audit (mu-specific addendum)

| Invariant | Held? | Notes |
|---|---|---|
| INV-1 (autonomy default Disallowed; opt-in only) | **Y** | `AutonomyCapability::default() == Disallowed` unchanged (capability.rs:86-90). Dispatch.rs validates Allowed before sending input. Loop-side defensive re-check verified by test `a3_disallowed_defensive_callout_no_autonomy_events`. |
| INV-2 (bounds enforced server-side, not by the model) | **Y** | `loop_.rs:930-947` reads `max_iterations` / `max_wall_clock_ms` / `max_total_tool_calls_in_autonomy` from `capability.autonomy` (NOT from `options`) at every iteration boundary. Options can NARROW via `min()` but cannot widen. Test `a1_iteration_cap_terminates_at_capability_bound` verifies cap-from-capability with `options.max_iterations: None`. |
| INV-3 (autonomy doesn't escape sandbox) | **Y** | Tool dispatch in autonomous mode still flows through the unchanged `handle_execute_tools` → `capability.check_allow()` / `capability.consume_tool_call()`. No tool-allowlist bypass introduced. |
| INV-4 (escalate-on-idle) | **OUT OF SCOPE** | `options.escalate_on_idle_after_ms` is accepted but no timer wired yet (Phase B follow-up or Phase D scope per spec). |
| INV-5 (sleeping consumes no model budget) | **OUT OF SCOPE** | Phase C territory (mu-7zn). |
| INV-6 (every iteration in event log) | **Y** | `AgentEvent::AutonomousIterationStarted/Completed` emitted at iteration boundaries (loop_.rs:599-603, 917-921, 1006-1010). Forwarder.rs `to_log_event` now lands them in the durable event log via the 3 new arms. |
| INV-7 (`AutonomousTerminated` is the final autonomy event) | **Y** | MaybeFinish autonomous branch emits `AutonomousTerminated` immediately before `mode = RunMode::Idle` (loop_.rs:972-996). Test assertions inside `a1` and `a2` (`last_autonomy_event(&events)` must be `AutonomousTerminated`). |

## Spec drift check (mu-specific addendum)
- All trait / wire / EventPayload changes have matching spec updates? **YES, no drift.**
  - The 3 new wire-event structs (`AutonomousIterationStartedEvent` / `Completed` / `Terminated`) implement the JSON shapes already documented at `specs/mu-036-session-autonomous-loop.md` lines 132-150 in prose. Adding Rust structs that match the existing spec-documented JSON shape is **implementing**, not **drifting** — criterion #9 does NOT fire (pre-confirmed in experiment 1 briefing).
  - The 3 new `AgentEvent` variants mirror the spec's "New AgentEvent variants" line 47.
  - The `AgentInput::StartAutonomous` variant is an internal protocol shape (not on the wire); spec describes its purpose at lines 52-58.
  - The commit message body for the Phase B commit explicitly cites spec lines 132-150 to make the implements-not-drifts relationship auditable.

## Skill / protocol updates suggested (post-run)
- **`jj split <paths>` is the load-bearing primitive** for separating concerns post-hoc when working in jj. It belongs in the goal-protocol skill's "useful incantations" section if not already. The pattern: keep working until the right moment, then split the working copy revision into selected (parent, keeps the description) + remaining (new child, no description). Cleaner than git's interactive add.
- **Calibration data point**: experiment 2 spent ~$8 of $15 on (a) finishing ~30% of Phase B = ~$3, (b) writing 3 FauxProvider tests = ~$1.5, (c) Stretch A spec amendment = ~$2, (d) Stretch B 3 beads = ~$0.5, (e) briefings + termination = ~$1. The $15 cap is well-calibrated for "finish a Phase + two stretches" shape. Pro-rata for "single-Phase finish only" looks like $4-5 (which lines up with experiment 1's pre-cap rate).
- **Inherited dirty working copy worked cleanly as an experiment shape** — no criterion #6 false positive. The "experiment 1 left ~280 LOC; experiment 2 inherits" pattern is reproducible if it ever becomes useful again (e.g., budget-bounded follow-on sessions).
- **The wire-stub-before-implementation pattern (Phase A.2 stub → Phase B behavior) paid off**: the only change to `handle_start_autonomous`'s signature was sync → async + body rewrite; all the wire/serde plumbing was already in place. Recommend continuing this pattern for Phase C and Phase D.
