# mu: goal session 2026-05-29 — t4c forward build (mu-kex4)

Productized the t4c discovery prototype into a real Rust crate `crates/t4c`.
Supervised-foreground /goal. Branch `t4c-forward-build` off `main`, **commit-only,
UNPUSHED**. Report: `~/src/claude-personal/scripts/t4c-notes.md`. Memory:
`t4c-rust-crate-phase1-complete`. Experiment doc:
`~/.claude-personal/experiments/goal-2026-05-29-mu-t4c-forward-build.md`.

## What landed

| Commit | Bead | One-line |
|---|---|---|
| `zxplotln` | mu-kex4.1 | scaffold leaf crate (lib + bin) |
| `nlmrowrt` | mu-kex4.2 | registry-as-trait + shallow path-addressed tree (+ Ranker floor) |
| `skrvksvv` | mu-kex4.3 | CLI surface — find/walk/help/run, bare-path routing, JSON, fuzzy-fallback |
| `ysosrvur` | mu-kex4.4 | discover — curated catalog ∩ env, self-configuring TOML |
| `qpprpysq` | mu-kex4.5 | --help-ai --json standard + clap/shell emitters + self-registration |
| (this) | mu-kex4.7 | report (t4c-notes.md) + this brief |

Branch tip: `t4c-forward-build`. The `/btw` mu-solo stack (`oqsrmtwr`) was left
untouched throughout (parallel sibling off main).

## Test state
`TMPDIR=~/tmp cargo test -p t4c` → **24 passed, 0 failed**. `cargo clippy -p t4c
--all-targets` clean. Leaf invariant verified: `cargo build -p t4c` standalone,
zero workspace-sibling path-deps. (TMPDIR=~/tmp is REQUIRED — /tmp is noexec.)

## Goal status
- **mu-kex4** (tracking): **partial** — phases 1 + 2 complete & verified; phase 3 gated.
- Closed: mu-kex4.1, .2, .3, .4, .5, .7.
- Open/gated: **mu-kex4.6** (phase 3, mu-native integration) — see below.

## Stop criteria that fired
**#2 (cross-cutting architectural decision)** — by design, at the phase-3 gate.
Phase 3 wires t4c INTO mu (add t4c dep to a mu crate, implement `t4c::RegistrySource`
+ `Ranker` over mu internals, expose a daemon discovery method). The grounded seam
is recorded on bead mu-kex4.6 (full design in its comment). It was NOT implemented
because (a) it's a cross-cutting change to mu's deps + protocol surface, and (b) its
value — does discovery reduce flailing — is only confirmable by the **cold-bare-Claude
dogfood** + the **gpt-in-mu second opinion**, neither autonomously fakeable. Stopping
here is the designed success path, not a failure.

## Things noticed but not addressed
- **mu already has the manifest pieces** (revises the experiment's predicted stop
  #4): `ToolRegistry` (enumerable, `attenuate_with(cap)`), `Tool::spec()`,
  `Capability.allowed_tools`/`check_allow` (discovery-tracks-permission, in code),
  `skill::loader::discover_skills` + `skills::routing_index`, `RecallProvider`.
  Phase 3 is integration, not greenfield.
- **NFS safe-defaults:** catalog `bash.fd` invokes bare `fd`; on this host
  traversal tools want `--one-file-system` in their metadata. (File as a follow-up.)
- **`t4c find` vs unix `find`** naming collision — doc note worth adding.
- Folkloric-capabilities was live-reproduced this session (`agent memory get` guess)
  — captured in the report as the thesis's own proof.

## Suggested next session (human-led)
Take up mu-kex4.6 with the seam on its comment. Decompose into: (a) t4c dep +
`RegistrySource` over `ToolRegistry`; (b) skill source over `discover_skills`;
(c) `Ranker` over `RecallProvider`; (d) daemon JSON-RPC `capabilities`/`discover`
surface + `mu` CLI subcommand; (e) the two human validations. Then push the branch
and open the PR once (a)-(d) land and (e) passes.

## Cost / turns / wall-clock
Supervised foreground (not budget-metered). One sitting; ~1900 LOC across 6 commits,
24 tests. Interruptions handled inline (locatedb/NFS scare → diagnosed as a benign
root setuid scan, not ours).
