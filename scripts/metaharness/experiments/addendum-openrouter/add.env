# Path A profile — OpenRouter deepseek-v4-pro, tool-dialect addendum. BILLED (~pennies).
# Addendum delivered via dispatch SYSPROMPT (provider-agnostic, works pre-merge). The
# production-equivalent is slice #4's catalog system_prompt_addendum (PR #376): once
# merged + the binary rebuilt, A/B the catalog field via a MU_MODELS_ overlay here.
PROFILE_ID="or-add"
PROVIDER="openrouter"
MODEL="deepseek/deepseek-v4-pro"
THINKING="low"
SYSPROMPT="$MH_ROOT/experiments/addenda/tool-dialect-nudge.txt"
