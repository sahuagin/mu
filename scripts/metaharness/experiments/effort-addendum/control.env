# Knob profile — control: low effort, no addendum. Model is resolved by ROLE
# (default harness_fit) at the loop level — NEVER set a provider/model here.
PROFILE_ID="control"
THINKING="low"
SYSPROMPT=""
